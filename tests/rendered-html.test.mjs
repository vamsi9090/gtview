import assert from 'node:assert/strict';
import test from 'node:test';
const cases=[['/','You’re invited to'],['/before-you-start','A good setup starts here.'],['/unbox','Meet your kit.'],['/install','Four simple steps to road-ready.'],['/alignment-check','Give the road a clear view.'],['/connect-and-test','Connected. Checked. Confirmed.'],['/review','Take one final look.'],['/complete','Restoring your record'],['/safety','Keep the setup safe.'],['/troubleshooting','A little help for every bump.'],['/support','Let’s get you the right help.'],['/faq','What would you like to know?'],['/privacy','Your questions about imagery.'],['/accessibility','Make yourself comfortable.']];
test('every requested route serves its own real page and shared navigation',async()=>{const {default:worker}=await import('../dist/server/index.js');assert.equal(typeof worker.fetch,'function');for(const[path,heading]of cases){const response=await worker.fetch(new Request('http://localhost'+path,{headers:{accept:'text/html'}}),{ASSETS:{fetch:async()=>new Response('Not found',{status:404})}},{waitUntil(){},passThroughOnException(){}});assert.equal(response.status,200,path);const html=await response.text();assert(html.includes(heading),path+' heading');assert(html.includes('Skip to main content'),path+' skip link');assert(!html.includes('Starter Project'),path+' starter cleanup');}});
